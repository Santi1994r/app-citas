import React from 'react'
import Button from 'react-bootstrap/Button';

const CitasButton = () => {
  return (
    <Button className='buttonCitas' variant="primary">{">>"}</Button>
  )
}

export default CitasButton;
